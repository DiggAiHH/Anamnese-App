diff --git a/node_modules/@react-native-windows/cli/lib-commonjs/utils/msbuildtools.js b/node_modules/@react-native-windows/cli/lib-commonjs/utils/msbuildtools.js
index bfc63f6..14e4c93 100644
--- a/node_modules/@react-native-windows/cli/lib-commonjs/utils/msbuildtools.js
+++ b/node_modules/@react-native-windows/cli/lib-commonjs/utils/msbuildtools.js
@@ -86,8 +86,9 @@ class MSBuildTools {
         const logPrefix = path_1.default.join(buildLogDirectory || os_2.default.tmpdir(), `msbuild_${process.pid}_${target}`);
         const errorLog = logPrefix + '.err';
         const warnLog = logPrefix + '.wrn';
-        const localBinLog = target === 'build' ? '' : ':deploy.binlog';
-        const binlog = buildLogDirectory ? `:${logPrefix}.binlog` : localBinLog;
+        // Always write a unique binlog file to avoid collisions with the default
+        // "msbuild.binlog" (which can be locked by another process).
+        const binlog = `:${logPrefix}.binlog`;
         const args = [
             `/clp:NoSummary;NoItemAndPropertyList;Verbosity=${verbosityOption}`,
             '/nologo',
diff --git a/node_modules/@react-native-windows/cli/lib-commonjs/utils/vsInstalls.js b/node_modules/@react-native-windows/cli/lib-commonjs/utils/vsInstalls.js
index 622a7ec..9e3bc46 100644
--- a/node_modules/@react-native-windows/cli/lib-commonjs/utils/vsInstalls.js
+++ b/node_modules/@react-native-windows/cli/lib-commonjs/utils/vsInstalls.js
@@ -29,9 +29,14 @@ function vsWhere(args, verbose) {
     if (!fs_1.default.existsSync(vsWherePath)) {
         throw new telemetry_1.CodedError('NoVSWhere', `Unable to find vswhere at ${vsWherePath}`);
     }
-    const system32 = `${process.env.SystemRoot}\\System32`;
-    const cmdline = `${system32}\\cmd.exe /c ${system32}\\chcp.com 65001>nul && "${vsWherePath}" ${args.join(' ')} -format json -utf8`;
-    const json = JSON.parse((0, child_process_1.execSync)(cmdline).toString());
+    // Use execFileSync instead of a cmd.exe wrapper.
+    // The previous implementation used cmd.exe to force UTF-8 codepage, but cmd
+    // parsing breaks version ranges containing parentheses (e.g. [17.11,18.0)).
+    // Calling the executable directly avoids shell parsing issues entirely.
+    const jsonText = (0, child_process_1.execFileSync)(vsWherePath, [...args, '-format', 'json', '-utf8'], {
+        encoding: 'utf8',
+    });
+    const json = JSON.parse(jsonText.toString());
     for (const entry of json) {
         entry.prerelease = entry.catalog.productMilestoneIsPreRelease;
     }
@@ -42,6 +47,9 @@ function vsWhere(args, verbose) {
  */
 function enumerateVsInstalls(opts) {
     const args = [];
+    // Explicitly search all product SKUs. Without this, vswhere may return no
+    // results even when Build Tools / Community are installed.
+    args.push('-products', '*');
     if (opts.minVersion) {
         // VS 2019 ex: minVersion == 16.7 => [16.7,17.0)
         // VS 2022 ex: minVersion == 17.0 => [17.0,18.0)
@@ -69,10 +77,11 @@ function enumerateVsInstalls(opts) {
         if (opts.verbose) {
             console.log(`Looking for VS installs with version range: ${versionRange}`);
         }
-        args.push(`-version ${versionRange}`);
+        // Pass as separate argv entries (required for execFileSync).
+        args.push('-version', versionRange);
     }
     if (opts.requires) {
-        args.push(`-requires ${opts.requires.join(' ')}`);
+        args.push('-requires', ...opts.requires);
     }
     if (opts.latest) {
         args.push('-latest');
