const React = require('react');
const { Text } = require('react-native');

const DatePicker = () => React.createElement(Text, null, 'DatePicker is not supported on web.');

module.exports = DatePicker;
module.exports.default = DatePicker;
