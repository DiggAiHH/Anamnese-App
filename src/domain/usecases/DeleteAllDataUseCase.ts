/**
 * @deprecated This use case was moved to the application layer.
 * Import from src/application/use-cases/DeleteAllDataUseCase instead.
 */
export interface DeleteAllDataUseCase {
  execute(): Promise<void>;
}
